from core.modules import Modules

if __name__ == "__main__":
    """
    That's all the main will need.
    Just run the load method to detect and launh all plugins
    """
    Modules().load()
